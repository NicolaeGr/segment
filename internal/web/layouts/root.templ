package layouts

import (
	"strings"

	"example.com/segments/internal/web/seg"
)

templ RootSegment(child templ.Component) {
	<html lang="en">
		<head>
			<meta charset="utf-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1"/>
			<title>{ seg.PageTitle(ctx) }</title>
			<link rel="stylesheet" href="/static/output.css"/>
			<script src="/static/theme.js"></script>
			<script defer src="https://unpkg.com/htmx.org@4.0.0/dist/htmx.min.js"></script>
			<script defer src="https://unpkg.com/alpinejs@3.14.9/dist/cdn.min.js"></script>
			<script type="module" src="https://unpkg.com/@oddbird/css-anchor-positioning"></script>
			<script src="/static/app.js" defer></script>
		</head>
		<body data-stack={ stackAttr(seg.StackIDs(ctx)) }>
			<div id="outlet-root">
				@child
			</div>
			<div id="modal-root"></div>
		</body>
	</html>
}

func stackAttr(ids []string) string {
	if len(ids) == 0 {
		return "root"
	}
	return strings.Join(ids, ",")
}
